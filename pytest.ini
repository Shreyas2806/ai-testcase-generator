[pytest]
# Tell pytest to only search for tests in the app/tests directory.
testpaths = app/tests

# Add project root to python path so the 'app' module imports work
pythonpath = .

# Standard test patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*
